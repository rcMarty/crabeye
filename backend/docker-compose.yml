services:
  postgres:
    image: postgres:14-alpine
    ports:
      - "5431:5432"
    volumes:
      - ~/apps/postgres:/var/lib/postgresql/data
    environment:
      - POSTGRES_PASSWORD=S3cret
      - POSTGRES_USER=crabeye_user
      - POSTGRES_DB=crabeye_db


volumes:
  db_data:
    driver: local
